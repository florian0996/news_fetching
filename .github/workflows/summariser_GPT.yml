name: summarise with GPT

on:
  # Allow manual triggers
  workflow_dispatch:
  # Run all summaries every day at 08:00 UTC
  schedule:
    - cron: '0 8 * * *'

jobs:
  summarise:
    runs-on: ubuntu-latest

    env:
     OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}

    steps:
      - name: Check out repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'
          
      - name: Upgrade pip
        run: python -m pip install --upgrade pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install openai PyYAML pandas

      - name: Run daily summary
        run: |
          python scripts/create_summaries_GPT.py --mode daily

      - name: Run weekly summary (Mondays only)
        run: |
          if [ "$(date -u +%u)" = "1" ]; then
            python scripts/create_summaries_GPT.py --mode weekly
          else
            echo "Not Monday, skipping weekly summary."
          fi
